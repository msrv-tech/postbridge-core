{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Postbridge Desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-open"]
}
